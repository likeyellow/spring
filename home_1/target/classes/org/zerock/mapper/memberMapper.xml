<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> -->


<!--  	이 Mapper 대신 
			src/main/resources/mappers 에 있는 BoardMapper.xml 와
			org.zerock.persistence/BoardMapper.java(<<인터페이스임) 
			 사용함
 -->
<!-- <mapper namespace="org.zerock.mapper.memberMapper">

	<select id="getTime" resultType="string">
		select now()
	</select>

	<insert id="insertMember">
		insert into tbl_member3(userid, userpw, username, email) values
		(#{userid}, #{userpw}, #{username}, #{email})
	</insert>


	<select id="selectMember"
		resultType="org.zerock.domain.MemberVO">
		select * from tbl_member3 where userid = #{userid}
	</select>
	
	<select id="readWithPW"
		resultType="org.zerock.domain.MemberVO">
		select * from tbl_member3 where = #{userid} and userpw = #{userpw}
		</select>
</mapper>				 -->